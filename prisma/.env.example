# Local DATABASE_URL SSOT = repository root `.env`
# Do not put DATABASE_URL here — Prisma CLI loads schema-adjacent .env and conflicts with root.
# Use: pnpm prisma:validate / prisma:generate / prisma:migrate:deploy from repo root.
