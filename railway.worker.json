{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "preDeployCommand": ["pnpm exec prisma migrate deploy --schema=prisma/schema.prisma"],
    "restartPolicyType": "NEVER"
  }
}
